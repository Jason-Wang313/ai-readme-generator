#include <stdio.h>


int main()
{

    int a, b;
    int sum;//a new variable to store the result
    printf("Hello world!\n");
    scanf ("%d%d", &a, &b);
    sum= a + b;//perform the calculation after getting the values
    printf("The sum of %d and %d is %d\n",a,b,sum );




    return 0;
}
