#include <stdio.h>


int main()
{

    int a, b, c;
    int product;//a new variable to store the result
    printf("Hello world!\n");
    scanf ("%d%d%d", &a, &b, &c);
    product= a * b * c ;//perform the calculation after getting the values
    printf("The product of %d, %d and %d is %d\n",a,b,c,product );




    return 0;
}

