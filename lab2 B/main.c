#include <stdio.h>
#include <stdlib.h>
int main()
{float p;
double q;
//part1: float P
printf("Enter value for float p: ");
    scanf("%f", &p);

    printf("p in decimal format = %f\n", p);
    printf("p in exponential format = %e\n", p);
//part2: double q
printf("Enter the value for double q: \n");
scanf("%lf", &q);
printf("q in decimal format: %f\n");
printf("q in exponential format: %e\n", p);
2*p+3*q;
printf("%f", 2*p+3*q);






    return 0;
}

